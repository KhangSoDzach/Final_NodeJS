<div class="admin-sidebar">
    <div class="admin-sidebar-header">
        <h2>Source Computer</h2>
    </div>
    
    <nav>
        <ul class="admin-nav">
            <li class="admin-nav-item">
                <a href="/admin" class="admin-nav-link <%= typeof path !== 'undefined' && (path === '/dashboard' || path === '/admin') ? 'active' : '' %>">
                    <i class="fas fa-tachometer-alt"></i> Dashboard
                </a>
            </li>
            
            <li class="admin-nav-item">
                <a href="/admin/products" class="admin-nav-link <%= typeof path !== 'undefined' && path.includes('/admin/products') ? 'active' : '' %>">
                    <i class="fas fa-laptop"></i> Sản phẩm
                </a>
            </li>
            
            <li class="admin-nav-item">
                <a href="/admin/orders" class="admin-nav-link <%= typeof path !== 'undefined' && path.includes('/admin/orders') ? 'active' : '' %>">
                    <i class="fas fa-shopping-cart"></i> Đơn hàng
                </a>
            </li>
            
            <li class="admin-nav-item">
                <a href="/admin/users" class="admin-nav-link <%= typeof path !== 'undefined' && path.includes('/admin/users') ? 'active' : '' %>">
                    <i class="fas fa-users"></i> Người dùng
                </a>
            </li>
            
            <li class="admin-nav-item">
                <a href="/admin/coupons" class="admin-nav-link <%= typeof path !== 'undefined' && path.includes('/admin/coupons') ? 'active' : '' %>">
                    <i class="fas fa-ticket-alt"></i> Mã giảm giá
                </a>
            </li>
            
            <li class="admin-nav-item">
                <a href="/" class="admin-nav-link">
                    <i class="fas fa-arrow-left"></i> Về trang chủ
                </a>
            </li>
            
            <li class="admin-nav-item">
                <a href="/auth/logout" class="admin-nav-link">
                    <i class="fas fa-sign-out-alt"></i> Đăng xuất
                </a>
            </li>
        </ul>
    </nav>
</div>
<!-- <style>
    :root {
      --sidebar-bg-start: #23272e;
      --sidebar-bg-end: #2b2d42;
      --sidebar-text-color: #fff;
      --sidebar-link-color: #e0e6ed;
      --sidebar-link-active-text: #fff;
      --sidebar-border-color: #374151;
      --sidebar-icon-color: #a3bffa;
      --sidebar-header-text-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);

      --radius-md: 8px;
      --radius-sidebar-default: 18px;

      --font-family-sans: system-ui, -apple-system, BlinkMacSystemFont,
        "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      --font-size-md: 1.08rem;
      --font-weight-medium: 500;

      --transition-short: 0.18s;
      --transition-medium: 0.2s;
    }

    /* Sidebar container */
    .admin-sidebar {
      background: linear-gradient(
        180deg,
        var(--sidebar-bg-start) 60%,
        var(--sidebar-bg-end) 100%
      );
      color: var(--sidebar-text-color);
      width: 250px;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      padding-bottom: 2rem;
      border-radius: 0 var(--radius-sidebar-default) var(--radius-sidebar-default) 0;
      box-shadow: 2px 0 16px rgba(44, 62, 80, 0.1);
      position: fixed;
      top: 0;
      left: 0;
      z-index: 1000;
      overflow-y: auto;
    }

    /* Sidebar header */
    .admin-sidebar-header {
      padding: 3rem 1rem 1.5rem;
      border-bottom: 1px solid var(--sidebar-border-color);
      text-align: center;
    }

    .admin-sidebar-header h2 {
      margin: 0;
      font-family: var(--font-family-sans);
      font-size: 1.7rem;
      font-weight: 700;
      letter-spacing: 2px;
      color: var(--sidebar-text-color);
      text-shadow: var(--sidebar-header-text-shadow);
      user-select: none;
    }

    /* Navigation */
    nav {
      flex-grow: 1;
      padding: 0 0.5rem;
    }

    ul.admin-nav {
      list-style: none;
      margin: 0;
      padding: 0;
    }

    li.admin-nav-item {
      margin-bottom: 0.5rem;
    }

    a.admin-nav-link {
      display: flex;
      align-items: center;
      gap: 1rem;
      padding: 0.85rem 1.5rem;
      font-family: var(--font-family-sans);
      font-size: var(--font-size-md);
      font-weight: var(--font-weight-medium);
      color: var(--sidebar-link-color);
      text-decoration: none;
      border-radius: var(--radius-md);
      transition:
        background-color var(--transition-short),
        color var(--transition-short),
        box-shadow var(--transition-short);
      user-select: none;
    }

    a.admin-nav-link i {
      font-size: 1.25rem;
      width: 24px;
      text-align: center;
      color: var(--sidebar-icon-color);
      transition: color var(--transition-medium);
      flex-shrink: 0;
    }

    /* Active and hover states */
    a.admin-nav-link.active,
    a.admin-nav-link:hover,
    a.admin-nav-link:focus {
      background: linear-gradient(
        90deg,
        var(--primary-color, #2196f3) 60%,
        var(--primary-color-dark, #0066cc) 100%
      );
      color: var(--sidebar-link-active-text);
      box-shadow: 0 2px 12px 0 rgba(33, 150, 243, 0.08);
      outline: none;
    }

    a.admin-nav-link.active i,
    a.admin-nav-link:hover i,
    a.admin-nav-link:focus i {
      color: var(--sidebar-link-active-text);
    }

    a.admin-nav-link:active {
      background: var(--sidebar-bg-start);
    }

    /* Last two nav items styling */
    li.admin-nav-item:nth-last-child(-n + 2) {
      margin-top: 1.5rem;
      border-top: 1px solid var(--sidebar-border-color);
      padding-top: 1rem;
    }

    /* Scrollbar styling for sidebar */
    .admin-sidebar::-webkit-scrollbar {
      width: 8px;
    }

    .admin-sidebar::-webkit-scrollbar-track {
      background: transparent;
    }

    .admin-sidebar::-webkit-scrollbar-thumb {
      background-color: rgba(255, 255, 255, 0.15);
      border-radius: 4px;
    }

    /* Responsive adjustments */
    @media (max-width: 768px) {
      .admin-sidebar {
        position: fixed;
        width: 220px;
        left: -220px;
        transition: left 0.3s ease;
        border-radius: 0 12px 12px 0;
      }

      .admin-sidebar.show {
        left: 0;
      }
    }
  </style> -->
